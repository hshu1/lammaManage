# Llama 控制台 - 极简美学与体验重构设计说明

## 1. 核心理念：极简美学与“去后台化” (De-dashboarding)

传统的侧边栏（Sidebar）或多级 Tab 导航虽然逻辑严密，但带有强烈的“企业级后台管理”气息，容易给用户带来认知负担。

根据 **人机交互中的极简主义美学（Minimalism in UX）** 以及 **席克定律（Hick's Law，选项越多做决定的时间越长）**，真正的优秀工具不应该让用户觉得“我在操作一个系统”，而是“我在使用一个简单的工具”。

**新的设计原则：**
1. **单点聚焦（Single Focus）**：打破“控制台”和“模型库”的页面割裂。用户打开软件的核心诉求只有一件事情：“选一个模型，跑起来”。
2. **渐进式展示（Progressive Disclosure）**：隐藏低频操作（如下载、设置），只在需要时通过弹窗或抽屉（Drawer）呼出，保持主界面的极致干净。
3. **消除导航栏（Zero Navigation）**：不再需要复杂的菜单，用“一站式单页”解决所有核心流程。

---

## 2. 颠覆性架构：一站式瀑布流体验

我们将废除目前的四大 Tab（控制台、模型库、下载、设置），将 UI 重组为一个**高度聚焦的单页应用程序（Single Page View）**。

### 视觉层次布局示意：

```text
+-------------------------------------------------------------------------+
| Llama.cpp 控制台 (纯文字)                            [➕下载模型] [⚙️ 设置] |
+-------------------------------------------------------------------------+
|                                                                         |
|  +-------------------------------------------------------------------+  |
|  |                         【核心状态岛】                              |  |
|  |                                                                   |  |
|  |  (未运行) 💤 服务处于休眠状态                                        |  |
|  |  (运行中) 🟢 Qwen-7B 正在运行 | PID: 1234 | 127.0.0.1:8080 [WebUI] |  |
|  +-------------------------------------------------------------------+  |
|                                                                         |
|                                                                         |
|  【你的本地大模型】(直接平铺在下方)                                          |
|                                                                         |
|  +-------------------------------------------------------------------+  |
|  | 📦 qwen2.5-coder-7b.gguf                                [ 🚀 启动 ] |  |
|  +-------------------------------------------------------------------+  |
|  | 📦 llama-3-8b-instruct.gguf                             [ 🚀 启动 ] |  |
|  +-------------------------------------------------------------------+  |
|                                                                         |
|   *(点击 [启动] 后，原地平滑展开参数配置面板，确认后直接拉起服务)*              |
|                                                                         |
+-------------------------------------------------------------------------+
```

---

## 3. 功能模块归属与动线重塑

在极简美学下，操作动线被极大缩短：

### 3.1 核心状态岛 (Dynamic Status Island)
借鉴苹果的 Dynamic Island 理念。
* 位于页面最上方正中央。
* **服务停止时**：它是一个极其低调的灰色占位符。
* **服务运行时**：它会变为绿色/工业蓝高亮卡片，直接展示“模型名称、端口、停止按钮、打开 WebUI”。这成为了整个页面的视觉重心。

### 3.2 列表即操作 (List is Action)
* 废除单独的“Dashboard”页面，直接把“本地模型列表”作为主页的主体内容。
* 用户一进入页面，就能看到自己有哪些模型。
* 每个模型卡片右侧直接有一个显眼的 **“启动 (Launch)”** 按钮。
* **参数配置**：点击“启动”时，模型卡片向下平滑展开（Accordion 形式），露出“GPU 层数、上下文长度”等设置，用户点击确认后即刻启动。做到**操作不出当前上下文**。

### 3.3 低频功能隐性化 (Hidden Secondary Features)
* **下载 HF 模型**：变为右上角的一个极简图标按钮，点击后从右侧滑出抽屉（Drawer）或居中弹出模态框（Modal）。下完后立刻反映在主页的模型列表中。
* **系统设置**：变为右上角的齿轮图标 `⚙️`，同样采用弹窗设计。绝不占用主界面的常规空间。

---

## 4. UI 留白与排版规范

1. **大留白（Macro Whitespace）**：模块之间增加至少 `32px` 或 `48px` 的间距，让内容能够“呼吸”。
2. **无边框设计（Borderless Design）**：尽量取消生硬的边框线（Border），改用非常微妙的背景色差或极度克制的阴影来区分层级，营造“悬浮”与“轻盈”的质感。
3. **文字层级（Typography Hierarchy）**：
   * 一级信息（模型名称、运行状态）：字重 `600/700`，颜色使用绝对纯度高的工业蓝或白/黑色。
   * 二级信息（文件大小、路径）：字重 `400`，使用浅灰色 `var(--text-muted)`。

## 总结
通过这套“一站式单页 + 状态岛 + 渐进式参数展开”的设计方案，我们彻底抛弃了繁琐的“后台管理”思维，让软件回归最原始纯粹的工具属性：**选定模型，点击启动，即刻使用**。
