# LammaManage 当前存储设计方案

> **核心原则**：出厂默认参数放 **JSON**（静态只读），个人与易变数据放 **SQLite**（动态读写）。  
> **优先级规则**：**有个人参数时用个人参数（覆盖默认值）；无个人参数时用 JSON 初始化参数。**

---

## 1. 存储分工图解

```mermaid
flowchart LR
    A[系统运行 / 前端读取] --> B{SQLite 是否有个人配置?}
    B -- 有 --> C[✅ 使用 SQLite 个人参数 (覆盖默认)]
    B -- 无 --> D[📦 使用 JSON 出厂初始化参数]
    
    E[用户修改设置 / 启动模型] -->|写入持久化| F[(SQLite 数据库)]
    F -.->|只读不污染| G[JSON 默认配置文件]
```

---

## 2. 存储介质划分

| 存储介质 | 对应文件 | 存储内容 | 读写权限 |
| :--- | :--- | :--- | :--- |
| **JSON 初始化配置** | `server/defaultConfig/config.default.json` | 出厂默认端口(`8080`)、默认Host(`127.0.0.1`)、内置6套启动方案、初始启动参数 | **只读**（出厂基准模板） |
| **JSON 默认收藏夹** | `server/defaultConfig/bookmarks.default.json` | 出厂预置推荐的高性能 GGUF 大模型列表（Qwen3.8、DeepSeek Flash、Coder 等） | **只读**（收藏夹基准模板） |
| **SQLite 数据库** | `server/config/lamma.db` | 1. **个人设置**（自定义路径、端口、当前激活模型、最新启动参数）<br>2. **自定义预设方案**<br>3. **模型收藏夹** | **动态读写**（高频持久化） |

---

## 3. SQLite 数据表结构

数据库基于 Node.js 原生 `node:sqlite` 实现，无需任何外部 C++ 编译依赖，包含以下 3 张表：

### ① 个人设置表 (`user_settings`)
存储用户自定义的各种配置项（键值对形式）：
* `key` (主键): 配置项名称，如 `executablePath`、`defaultPort`、`activeModel`、`launchParams` 等。
* `value`: 配置项值（JSON 字符串）。
* `updated_at`: 更新时间。

### ② 预设方案表 (`presets`)
存储用户自定义或修改后的启动方案：
* `id` (主键): 方案唯一标识（如 `scheme_custom_1`）。
* `name`: 方案名称。
* `params`: GPU层数、上下文大小(ctxSize)、线程数等参数。
* `is_custom`: 是否为用户自建方案。

### ③ 模型收藏夹表 (`bookmarks`)
存储从 HuggingFace 收藏的模型列表及磁盘文件大小：
* `id` (主键): 收藏条目 ID。
* `name` / `filename`: 模型名称与文件名。
* `repo_id`: HuggingFace 仓库路径。
* `size`: 模型体积（自动与本地磁盘或远程同步）。

---

## 4. 关键功能与逻辑

1. **自动合并与覆盖**：
   - 启动或查询时，系统先加载 JSON 默认配置作为底色。
   - 检查 SQLite 中的记录，若存在则逐项覆盖对应字段。
2. **安全隔离**：
   - 用户在页面修改配置、切换模型或添加/删除收藏时，仅写入 SQLite，永远不会污染 JSON 初始文件。
3. **一键恢复出厂默认**：
   - **系统配置重置**：点击「恢复出厂初始化配置」时，清空 SQLite 个人参数表，系统配置立即回退至 `config.default.json`。
   - **收藏夹重置**：点击「恢复默认推荐」时，重置 SQLite 收藏夹表为 `bookmarks.default.json` 中的初始推荐模型列表。
4. **历史数据无缝迁移**：
   - 首次启动且数据库为空时，自动将历史 `bookmarks.json`（或 `bookmarks.default.json`）载入至 SQLite 数据库中。
